#ごみを出せるかどうかを判定するメソッドを作りたい

module Policy
end

class Policy::CheckAbleskip
  current_user==@history.user &&((Date.today< @history.gomidasibi)||( (Date.today == @history.gomidasibi) &&('18:00' > Time.zone.now.to_s(:time)
end
